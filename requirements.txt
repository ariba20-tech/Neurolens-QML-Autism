# NeuroLens — Full Stack Requirements
# =====================================
# Install: pip install -r requirements.txt

# Core ML
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# Quantum ML (optional — pure numpy fallback works without it)
# pennylane>=0.36

# Explainability (optional — built-in fallback works without it)
# shap>=0.44

# FastAPI Backend
fastapi>=0.110.0
uvicorn>=0.27.0
httpx>=0.27.0
pydantic>=2.0

# Visualization
matplotlib>=3.8

# Optional: Supabase Python client
# supabase>=2.0
